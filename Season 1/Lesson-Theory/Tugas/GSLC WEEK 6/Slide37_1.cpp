#include <stdio.h>
#include <ctype.h>

int main(){

    // isalpha(int c); = untuk memeriksa apakah sebuah karakter termasuk huruf atau bukan
    // char c = 'a';
    // if (isalpha(c) == 0){
    //     printf("%c is not an alphabet .", c);
    // }else{
    //     printf("%c is an alphabet.", c);
    // }


    // isupper(int c); = untuk memeriksa apakah sebuah karakter termasuk huruf besar atau bukan
    // char c ='A';
    // if (isupper(c)==0){
    //     printf("%c is not an uppercase.",c);
    // }else{
    //     printf("%c is an upercase",c);
    // }
    

    // islower(int c); = untuk memeriksa apakah sebuah karakter termasuk huruf kecil atau bukan
    // char c ='a';
    // if (islower(c)==0){
    //     printf("%c is not an lowercase.",c);
    // }else{
    //     printf("%c is an lowercase",c);
    // }


    // isdigit(int c); = untuk memeriksa apakah sebuah karakter termasuk angka atau bukan
    // char c = '8';
    // if (isdigit(c) == 0){
    //     printf("%c is not a digit.", c);
    // }else{
    //     printf("%c is a digit.", c);
    // }


    // isalnum(int c); = untuk memeriksa apakah sebuah karakter termasuk alpanumerik atau bukan
    // char c = '1';
    // if (isalnum(c) == 0){
    //     printf("%c is not an alphanumeric character.", c);
    // }else{
    //     printf("%c is an alphanumeric character.", c);
    // }
        
        
    // isspace(int c); = untuk memeriksa apakah sebuah karakter termasuk whitespace
    // char c = '\n';
    // if (isspace(c) == 0){
    //     printf("%c is not an white-space character.", c);
    // }else{
    //     printf("%c is an white-space character.", c);
    // }


    // toupper(int c); = untuk mengubah huruf kecil menjadi huruf besar
    // char c = 'x';
    // printf("%c",toupper(c));

    // tolower(int c); = untuk mengubah huruf besar menjadi huruf kecil
    // char c = 'X';
    // printf("%c",tolower(c));










    return 0;
}