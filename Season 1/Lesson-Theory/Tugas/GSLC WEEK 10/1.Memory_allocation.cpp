#include <stdio.h>

int  main(){

    // void *malloc ( size_t size );
    // - malloc() = is used to allocate one block of memory dynamically at heap memory.
    //   stating the size of allocated memory (in byte)
    // -  ( size_t size ) = size of memory in byte.


    // void *calloc ( size_t num, size_t size );
    // - calloc() = is used to allocate some blocks of memory dynamically at heap memory, 
    //   where each element given size.
    // - size_t num = number of byte each block or element 
    // - size_t size  = number of byte each block or element 


    // void *realloc ( void *memblock, size_t size );
    // - realloc() is used to reallocate some blocks of memory or element dynamically at heap. 
    // - void *memblock = is a pointer to the address of targeted memory location
    // - size_t size  = number of byte each block or element 

    // void free ( void *memblock );
    // - free() =  used to release allocated memory at heap memory.
    // - void *memblock = is a pointer to the address of targeted memory location








    return 0;
}